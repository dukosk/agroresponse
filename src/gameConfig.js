const DEFAULT_EVENT_SLUG = 'agrokomplex-nitra';
const MAPS = ['orchard', 'forest', 'field'];
const TIMER_SECONDS = 45;
const GRID_SIZE = 16;

module.exports = {
  DEFAULT_EVENT_SLUG,
  MAPS,
  TIMER_SECONDS,
  GRID_SIZE,
};
